VITE_AIONUI_BACKEND_BASE_URL=https://api.example.com

# Optional local frontend mode. Keeps app code on real HTTP paths and intercepts requests in a Service Worker.
# VITE_AIONWEB_MOCK_MODE=intercept
